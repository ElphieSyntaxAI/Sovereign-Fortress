# Author roadmap (alias)

Canonical **single source of truth** for vision, lexicon, phases, tiers, and MSGF states:

**[`AUTHOR_ECOSYSTEM_ROADMAP.md`](./AUTHOR_ECOSYSTEM_ROADMAP.md)**

Supabase migrations that implement roadmap cooldown semantics reference that document. The legacy editorial column remains `p4_manuscripts.revision_status` (`p4_revision_status` enum).
