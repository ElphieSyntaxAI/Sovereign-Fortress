# packages/core

MSGF Core package.

Contains modularized exports for:
- P4 State Ledger
- Shadow Mode preflight
- HAL Consensus scoring
- Core legal + vertex + hot-layer support

This package is wired as a transitional compatibility layer while the root `lib/` implementation is migrated fully.

