# apps/web

Future marketing site + SaaS frontend host app.

Intended scope:
- Public marketing pages
- Pricing + checkout for plugin access
- Authenticated dashboard shell (consuming `packages/core` and `packages/ui`)

