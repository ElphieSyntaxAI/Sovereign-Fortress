import vaultPactBilateralMd from "@nda/vault-pact-bilateral.md?raw";

/** Counsel-approved revision date for the bilateral Vault Pact (sync with BFF `GET /api/legal/vault-pact`). */
export const VAULT_PACT_LAST_UPDATED_ISO = "2026-05-13";

export const VAULT_PACT_MARKDOWN = vaultPactBilateralMd;
