# Production: from repo root — docker build -f docker/Dockerfile.author-bff .
# Deploy: ./setup-author-cloud.sh  (docs/DEPLOY_PRODUCT_DOMAINS.md)
FROM alpine:3.20
RUN echo "Build from monorepo root: docker build -f docker/Dockerfile.author-bff ." >&2 && exit 1
